#!/bin/bash
#
# Hides hidden files (default - this is to re-hide after using `show-hidden-files`)
defaults write com.apple.finder AppleShowAllFiles NO
killall Finder /System/Library/CoreServices/Finder.app

